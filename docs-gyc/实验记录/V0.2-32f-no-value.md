# V0.2 —— 32 帧，无 value

RoboTwin 双臂 14D，AC 后训练，**生成 32 帧**，224×224。V0.1 的"长 chunk"版本。

跟 V0.1 的差别：
- `num_action_per_chunk`: 16 → **32**（生成帧数翻倍）
- `state_t`: 5 → **9**（= 1 + 32/4）

其他全继承 V0.1：`fps_downsample_ratio=1`, 224×224, v1 数据。

| 参数 | 值 | 备注 |
|---|---|---|
| Wandb link | [moruoguo5589/cosmos_predict2_action_conditioned/mpepbct7](https://wandb.ai/moruoguo5589/cosmos_predict2_action_conditioned/runs/mpepbct7) | |
| FPS | 30（内容）/ 4（条件 tag，跑训练时 dataset 还是硬编码 4，4-18 才改成 24）| RoboTwin 原生 30 fps，`fps_downsample_ratio=1`。AC 配置 `rope_enable_fps_modulation=False`，tag 是 dead passenger 不影响生成 |
| 条件帧 | 1 | 单视角，camera_head |
| 生成帧数 | **32** | `num_action_per_chunk=32` |
| 输入输出总帧数 | 33 | 1 cond + 32 generated |
| 分辨率 | 224 × 224 | |
| 需要有 value | False | `state_t=9`（1 cond + 8 video latent） |
| 动作空间 | 末端位姿 (xyz + rpy) + gripper，14D | 见 `scripts/convert_robotwin_to_cosmos.py:47-64` |
| 任务数据 | place_container_plate (100/100)<br>plate_object_basket (80/100) | v1 切分（pcp 全 train，val 仅 pob） |
| 训练量（epochs） | ~53 | 100k × 16 / ~30,000 clips（seq_len=33, stride=1） |
| 训练量（steps） | **100,000** | |
| Batchsize \| GPU_num | 2 \| 8GPU | global batch = 16 |
| 训练时间（小时 hour） | **38.5 h** | 2026-04-16 03:23 → 2026-04-17 17:48，实测 1.62 s/iter（比 V0.1 的 1.24 s/iter 慢，latent T 更大）|

## 启动脚本

`scripts/train_robotwin_v02_32frame_8gpu.sh`

## 状态

✅ 已完成。最终 loss **0.0314**（iter 100,000，04-17 17:48）。

未做生成质量评估——预期跟 V0.1 一样在 10-20k 后崩坏（同样 `fps_downsample_ratio=1`），需要扫 ckpt 验证。
